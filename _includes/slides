<p>Note: Slides don't work very well in mobile browsers (at least, not
yet!)</p>
<div class="slidesWrapper{{ include.aspectRatio }}">
<iframe width='100%' height='75%' 
        src="{{ include.content }}"
        frameborder='0' allowfullscreen></iframe>
</div>

